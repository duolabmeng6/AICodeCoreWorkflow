interface:
  display_name: "编程核心工作流"
  short_description: "按复杂度分级编排研究、计划、执行与独立评审，默认自动实施"
  default_prompt: "使用 $workflow 处理这个开发需求；默认在计划后自动实施，只有我主要要求讨论或确认计划时才等待。"
policy:
  allow_implicit_invocation: false
